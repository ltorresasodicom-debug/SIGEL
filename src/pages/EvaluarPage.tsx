import { Placeholder } from '@/components/Placeholder';

/** Asistente ciudadano de evaluación. */
export function EvaluarPage() {
  return (
    <Placeholder
      titulo="Crea tu evaluación"
      descripcion="Asistente ciudadano de 8 dimensiones con preguntas simples; cálculo del INGEL y persistencia en Supabase."
    />
  );
}
